【执行工具：Codex｜模型：GPT-5.6 Luna｜推理等级：中】

为当前项目完成本轮交接，让下一位执行者不依赖旧聊天即可继续。

读取 AGENTS、PROJECT_CONTEXT、CURRENT_STATUS、任务记录和本轮实际变更/测试。把已确认的稳定决定写到适当设计文档，把动态停点写入 CURRENT_STATUS；涉及模块时同步其 STATUS。保留已有内容和未提交工作，不添加未经验证的完成结论。

必须记录：版本、阶段、任务、完成与未完成、阻塞及解除条件、决策、变更文件、实际测试、风险/未知项、下一项可执行任务、工作目录、仓库、branch/commit 和授权范围。不可用的远端或测试证据如实说明。

允许本任务所需的交接文档更新；commit/push/main/pack/deploy 依当前用户授权，不从交接动作推导额外权限。复杂技术判断引用已有评审，未解决的高风险问题交相应路线继续处理。

输出 `PROJECT_HANDOFF_REPORT`：更新文件、真实 Git 状态、当前停点、下一任务、必须读取文件、阻塞及未覆盖验证。给下一位 Agent 的任务提示词第一行明确工具、模型和适用推理等级。
