【执行工具：Codex｜模型：GPT-5.6 Terra｜推理等级：高】

在当前新项目中，为本轮已选择的能力制定可交给 Cursor 的实施任务。复用标准蓝图和已有项目决定，不默认实现所有模块。

核验项目目录、仓库、branch/HEAD 和已有改动；读取 AGENTS、PROJECT_CONTEXT、CURRENT_STATUS、四个配置、docs/PROJECT_PLAN.md、framework/MODULE_ADOPTION_PLAN.md 及选中蓝图。

风险涉及身份/权限/真实凭据、支付退款、数据库/Migration、生产数据或复杂安全设计时，实际分析/评审任务应按项目配置切换到 Codex｜GPT-5.6 Sol｜推理等级：极高，并安排所需独立复核。不要把此默认提示词当作高风险设计已批准。

1. 明确本轮唯一业务目标，核对使用者、已有组件、技术栈、部署位置、数据归属和验收。只询问真正阻塞当前方案的缺口。
2. 选择复用组件、适配器或项目自有实现，记录实际来源/版本与限制；不伪造 APF 可安装模块或版本依赖。
3. 落实输入输出、错误、权限、并发、幂等、状态、失败恢复与测试。完整 Module 按模板建契约；项目内部的小能力保持适当规模。
4. 拆成先后明确的任务，逐项写工作目录、分支、允许/禁止路径、前置门禁、测试命令或具体验证方式、验收和 Git/pack/deploy 授权。
5. 更新项目状态；未通过 Spec/Architecture 或所需独立评审时，不安排越过门禁的实现。

本轮只写必要方案和任务文档，不接入云账户/生产数据，不运行真实交易。commit/push/main 按当前任务授权，不从模板推导权限。

输出 `MODULE_IMPLEMENTATION_PLAN`：选中能力、实际复用来源、契约/数据归属、依赖和任务顺序、测试/验收、风险和待决问题、允许的 Git 操作。附 Cursor 执行提示词，第一行写 `【执行工具：Cursor｜模型：Grok 4.6 High Fast】`；Cursor 只执行已定方案。
