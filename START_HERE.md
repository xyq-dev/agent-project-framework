# 从空目录开始一个项目

目标：把本仓库的开发标准放进你的新项目，使它从第一项需求到上线维护都能在 GitHub 留下可恢复的记录。

## 1. 准备两个位置

| 位置 | 内容 |
| --- | --- |
| 标准来源 | 本仓库某个已核对 commit 的只读副本 |
| 项目根目录 | 你的新项目最外层文件夹；可以为空，也可以只有 GitHub 初始化的 README |

标准仓库和业务项目使用不同目录、不同 Git remote。执行前核对目录、现有文件及 Git 状态；不把业务代码写回标准仓库。

只需先明确项目名称、解决的问题、主要用户、首版范围和硬约束。技术栈可以在架构阶段选择；不能先编造客户、收费规则、数据模型或已完成状态。

## 2. 初始化标准文件

使用 [新项目启动提示词](agent-prompts/NEW_PROJECT.md)，或人工按 [BOOTSTRAP_MANIFEST.yaml](templates/project/BOOTSTRAP_MANIFEST.yaml) 执行：

1. 固定标准来源的完整 commit SHA，确认该版本包含本指南与清单。
2. 将清单 `copies` 中的源文件复制到新项目对应目标路径；目录项包含其中所有文件，包含必要的点目录。
3. 只在清单 `render_files` 列出的文件中填写占位值。Markdown 正常填写；YAML 用解析器赋值并重新校验，不用未经转义的字符串替换写入特殊字符。
4. 记录项目身份、负责人及标准来源。新项目仓库尚未创建时，repository 字段可为空；明确记录下一步，不猜远端地址。
5. 核对所有新项目阶段为 `pending`，模块尚未选择，测试为 `NOT_RUN`。

复制的是项目模板与共享规范。APF 根目录的 README、PROJECT_CONTEXT、CURRENT_STATUS、Storage 模块以及 APF 自身的 project/workflow 进度均不作为新项目的对应文件。

文件已存在时先比较，逐项合并适用内容并保留项目事实；不整目录覆盖。不要求预建 `apps/`、`modules/`、数据库或某个语言的 package 文件。

## 3. 完成立项，再开发

初始化后，新项目的入口是 `README.md` 与 `docs/PROJECT_PLAN.md`：

- 写清问题、用户、首版范围、不做什么、可验证验收条件。
- 填写阶段裁剪表和能力选择表。按[模块实施方案](framework/MODULE_ADOPTION_PLAN.md)选择登录、权限、OSS 等蓝图，再用[模块方案提示词](agent-prompts/MODULE_PLAN.md)形成项目任务；候选能力不代表已实现。
- 更新本项目 `.agent-project/workflow.yaml` 的 mode、reason、status 与 evidence。
- Codex 输出明确设计与有边界的任务；Cursor 从已批准任务执行，避免重复分析。

详细阶段要求见 [项目开发手册](framework/PROJECT_PLAYBOOK.md)。初始化完成只代表标准文件就绪，不代表需求获批、代码可用或业务已上线。

## 4. 在 GitHub 留下完整过程

新项目仓库创建和首次推送按该项目任务授权执行。模板进入其默认分支后，用立项 Issue 管首版目标，用开发/缺陷 Issue 管任务，用分支与 PR 管变更；验收后再发布。日常做法见 [GitHub 协作流程](framework/GITHUB_WORKFLOW.md)。

源码和可版本化记录放 Git；大型设计文件可以放 Figma 等工具，GitHub 保存链接、版本、导出说明和评审结论；测试报告保存命令、commit 和运行链接。不要把秘密值或客户原始数据写进公开记录。

## 5. 已有项目如何采用

先读取该项目已有 AGENTS、README、状态、依赖清单和 Git 变更。保留原项目目录与工具链，只对缺失的流程/模板建立一次独立变更；冲突规则由当前任务与实际项目事实决定。不重置进度，不重做已验收需求和架构，不把采纳标准变成业务重构。

## 初始化验收

- 项目身份正确；origin 属于本项目，或明确尚未配置。
- 新项目所有必读文件与复制清单目标存在；相对链接可解析。
- 已记录标准源仓库、版本、完整 commit；没有继承 APF 的已完成状态。
- 首版需求和未知项有记录；下一项是需求/方案工作，不默认执行 Storage M1-B。
- GitHub Issue/PR 模板存在；其生效需要进入项目默认分支。
- 初始化报告列出工作目录、branch、文件、校验结果和 Git 操作；尚未执行项如实记录。
